PORT=5000
NODE_ENV=development

# PostgreSQL (Railway injects this when you link the Postgres plugin)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# JWT — set in Railway Variables for production (required for auth routes)
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=1d
